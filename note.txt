User getting loan offer integrating the API 

in the applyLoan component first we will get the loan offer integrating the backend api so that all the offers created by the admin will display to all users with apply now button .

Here's a step-by-step explanation of the code with a 35-line summary at the end:

1. **Imports and Setup**:
   - React hooks (useState, useEffect) for state management
   - useNavigate for routing
   - axiosInstance for API calls
   - API_PATHS for endpoint URLs
   - Icons from react-icons
   - DashboardLayout for the page structure

2. **State Management**:
   - `offers`: Stores loan offer data from API
   - `loading`: Tracks API request status
   - `error`: Stores any API errors
   - `filters`: Manages pagination and filtering (page, limit, loanType)
   - `totalPages`: Tracks total pages from API
   - `showFilters`: Controls filter visibility

3. **Data Fetching**:
   - useEffect hook fetches loan offers when filters change
   - Constructs query parameters from filter state
   - Handles loading states and errors
   - Updates offers and pagination data from response

4. **Filter Handling**:
   - `handleFilterChange`: Updates loanType filter and resets to page 1
   - Toggle for showing/hiding filter UI

5. **Pagination**:
   - `handlePageChange`: Updates current page with bounds checking
   - Smart page number rendering (shows 5 pages at most)
   - Previous/Next buttons with disabled states

6. **UI Components**:
   - Loading spinner during API requests
   - Error display for failed requests
   - Header with title and description
   - Filter dropdown with loan type options
   - Responsive grid of loan offer cards
   - Pagination controls when multiple pages exist
   - Empty state when no offers match filters

7. **Offer Card Details**:
   - Displays loan type, amount (formatted as currency)
   - Shows interest rate and term months
   - Documents list with download links
   - "Apply Now" button that navigates to application page

8. **Utility Functions**:
   - `formatCurrency`: Formats amounts as Nigerian Naira
   - Error handling with user-friendly messages

**35-Line Summary**:
1. LoanOffers component displays available loan products
2. Uses React hooks for state and side effects
3. Fetches data from API with pagination support
4. Implements filtering by loan type
5. Shows loading state during API calls
6. Handles and displays errors gracefully
7. Responsive grid layout for offer cards
8. Each card shows key loan details
9. Documents can be downloaded if available
10. "Apply Now" redirects to application page
11. Pagination handles large result sets
12. Dynamic page number calculation
13. Previous/Next buttons with disabled states
14. Currency formatting for Nigerian Naira
15. Filter toggle shows/hides filter options
16. Loan type dropdown updates results
17. Resets to page 1 when filters change
18. Smooth scroll to top on page change
19. Empty state for no results
20. Special offer badges on cards
21. Uses DashboardLayout for consistent UI
22. Active menu highlighting
23. Responsive design for all screens
24. Hover effects on interactive elements
25. Focus states for accessibility
26. Error boundary for API failures
27. Type safety for financial data
28. Document download links open in new tab
29. Clean separation of concerns
30. Reusable utility functions
31. Consistent styling with Tailwind
32. Proper component organization
33. Accessible form controls
34. Semantic HTML structure
35. Follows React best practices