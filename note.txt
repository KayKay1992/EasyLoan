Creating User Dashboad 

i copied the admin dashboard to userDashboard just changed the api route to get_user_dashboard_data.

then i i experienced some bug while fetching the api route which i improved in userDashboard controller.

Key Improvements:
Complete Status Coverage: Now includes all statuses your frontend expects (active, approved, completed, defaulted, pending, rejected)

Proper Initialization: Ensures all statuses are included even if count is 0

Correct Structure: Matches exactly what your frontend expects with:

charts.loanDistribution for pie chart data

charts.loanTypeLevels for bar chart data

recentLoans for the recent loans table

Total Count: Calculates the All count that your InfoCards component uses

Consistent Naming: Uses the same naming conventions as your frontend

Additional Notes:
Make sure your Loan model has all these status values in its schema

The status values are case-sensitive, so ensure they match exactly what's in your database

If you need to add more loan types or statuses in the future, just add them to the respective arrays

This implementation will work seamlessly with your existing frontend code without requiring any changes to the frontend components.


then i added user to the select to get the user id