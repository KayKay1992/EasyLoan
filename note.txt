Adding getAllLoan controller

It defines a function to retrieve all loan records from the database.

It checks if the user making the request is an admin.

If not an admin, it returns a "Not authorized" error with status 403.

If authorized, it fetches all loans and includes user name and email.

It sends the list of loans back in the response, sorted by newest first.