Defining the structure of auth controller

🧠 Notes:
asyncHandler handles async errors cleanly (install with npm i express-async-handler)

Each controller matches one of your routes

You can now replace the TODOs with logic like:

Hash password with bcrypt

Generate JWT

Fetch and update user data