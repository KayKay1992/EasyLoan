Adding DeleteLoan controller

Retrieves the loan by its ID from the database.

Returns a 404 error if the loan doesn't exist.

Checks if the requesting user is an admin.

If authorized, deletes the loan record from the database.

Sends a success response confirming the loan was deleted.