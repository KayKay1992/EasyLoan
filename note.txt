Creating users get loan detail UI page with api integration and some small bugs fixed in many pages.

Explaining UserLoanDetails component.

This React component displays detailed information about a user's loan

It uses React hooks (useState, useEffect, useParams, useNavigate) for state and routing

Fetches loan data from an API using axios when component mounts

Handles loading, error, and no-data states with appropriate UI feedback

Calculates loan end date based on start date and term months

Formats currency values in Nigerian Naira (NGN)

Displays loan status with color-coded badges

Shows basic loan info (ID, user, type, status, dates)

Displays financial details (amount, interest, term, payments)

Lists uploaded documents if available

Has a back button to return to previous page

Uses a dashboard layout component for consistent UI

Responsive design with two columns on larger screens

Formatted dates using date-fns library

Clean, organized UI with proper spacing and borders

Type-safe API calls with predefined paths

Error handling for missing ID or failed requests

Loading spinner during data fetch

Status-specific styling for better visual cues

Properly structured HTML with semantic classes

Conditional rendering for optional data fields

Capitalized display values for better readability

Document list with fallback for empty state

Reusable utility functions for formatting

Follows React best practices for component structure

Uses Tailwind CSS for styling

Properly manages component state

Clean separation of concerns (data, UI, logic)

Accessible error messages and alerts

Comprehensive view of all loan-related information